{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Create New Hiking Post</h1>

    {{ form_start(postForm) }}
        {{ form_widget(postForm) }}
        <br>
        <button class="btn btn-primary">
            Create !
        </button>
    {{ form_end(postForm) }}

{% endblock %}